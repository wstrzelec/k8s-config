apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: ssid-email-lookup
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: ssid-email-lookup
  policy:
    alphabetical:
      order: asc
